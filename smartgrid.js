import smartGrid from 'smart-grid'

let options = {
    /* our settings here */
}

smartGrid('path-to-your-folder', options)

// 1) npm i smart-grid
// 2) сформировать сетку с помощью команды node [filename]
